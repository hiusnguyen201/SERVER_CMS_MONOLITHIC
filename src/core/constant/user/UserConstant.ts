export enum USER_TYPE {
  USER = 'User',
  CLIENT = 'Client',
}

export enum USER_SORT_BY_VALUES {
  NAME = 'name',
  EMAIL = 'email',
  CREATED_AT = 'createdAt',
}

export enum USER_STATUS {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
  LOCKED = 'Locked',
}
